# 🍺 Birreria Digitale

Progetto Full-Stack (Angular, Spring Boot, PostgreSQL).

### Come avviarlo:
1. Clonare il repository: `git clone <url>`
2. Entrare nella cartella: `cd birreria-digitale-root`
3. Avviare con Docker: `docker compose up --build -d`
4. Aprire il browser su: `http://localhost`
